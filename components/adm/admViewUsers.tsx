import {AdmModes} from "@/app/(protected)/adm/page";

interface IAdmViewUsers {
    setMode: (m: AdmModes) => void
}

export const AdmViewUsers = ({ setMode }: IAdmViewUsers) => {
    return (
        <div></div>
    )
}
